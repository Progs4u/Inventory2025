mixin itemModal()
  div#createItemModal(class="fixed inset-0 bg-gray-600 bg-opacity-50 hidden")
    div(class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white")
      div(class="mt-3")
        h3(class="text-lg font-medium") Create New Item
        +itemForm()
      button(onclick="closeModal('createItemModal')" class="absolute top-0 right-0 mt-4 mr-4") ×

mixin containerModal()
  div#createContainerModal(class="fixed inset-0 bg-gray-600 bg-opacity-50 hidden")
    div(class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white")
      div(class="mt-3")
        h3(class="text-lg font-medium") Create New Container
        +containerForm()
      button(onclick="closeModal('createContainerModal')" class="absolute top-0 right-0 mt-4 mr-4") ×

mixin storageModal()
  div#createStorageModal(class="fixed inset-0 bg-gray-600 bg-opacity-50 hidden")
    div(class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white")
      div(class="mt-3")
        h3(class="text-lg font-medium") Create New Storage
        +storageForm()
      button(onclick="closeModal('createStorageModal')" class="absolute top-0 right-0 mt-4 mr-4") ×
